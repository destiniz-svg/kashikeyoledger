# The web frontend lives in frontend/. Build it from there and publish its dist.
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback — serve index.html for client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
